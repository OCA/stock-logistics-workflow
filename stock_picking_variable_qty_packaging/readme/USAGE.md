Configure a multi-step operation type to propagate variable quantities. When
the done package count is entered on the source operation, downstream reserved
move lines keep that count instead of recalculating it from the changed weight.
