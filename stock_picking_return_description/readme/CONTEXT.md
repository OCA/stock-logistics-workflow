When a generic product (e.g. "Consumables") is used as a placeholder and
the actual item is recorded in the move description (e.g. "Tissues"), the
return picking wizard gives no indication of what is actually being returned.