Go to *Inventory > Configuration > Settings* and add both the delivery
operation type and its return operation type to *Portal Visible Operations*,
so customers can navigate between a delivery and its returns.
