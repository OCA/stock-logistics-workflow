* Go to *Inventory > Configuration > Settings > Inventory*.
* Under *Inventory Adjustment Threshold Filter*, optionally set the
  default adjustment cost threshold and quantity difference threshold
  proposed on the threshold filter search panel.
