<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_stock_interwarehouse_transfer_form" model="ir.ui.view">
        <field name="name">stock.interwarehouse.transfer.form</field>
        <field name="model">stock.interwarehouse.transfer</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button
                        name="action_confirm"
                        string="Confirm"
                        type="object"
                        class="btn-primary"
                        invisible="state != 'draft'"
                    />
                    <button
                        name="action_cancel"
                        string="Cancel"
                        type="object"
                        invisible="state in ('done', 'cancelled')"
                    />
                    <field
                        name="state"
                        widget="statusbar"
                        statusbar_visible="draft,confirmed,in_transit,done"
                    />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button
                            name="action_view_pickings"
                            type="object"
                            class="oe_stat_button"
                            icon="fa-truck"
                            invisible="picking_count == 0"
                        >
                            <field
                                name="picking_count"
                                widget="statinfo"
                                string="Transfers"
                            />
                        </button>
                    </div>
                    <field name="company_id" invisible="1" />
                    <div class="oe_title">
                        <h1>
                            <field name="name" />
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field
                                name="warehouse_from_id"
                                readonly="state == 'done'"
                            />
                            <field name="warehouse_to_id" readonly="state == 'done'" />
                        </group>
                        <group>
                            <field name="location_id" readonly="state == 'done'" />
                            <field name="location_dest_id" readonly="state == 'done'" />
                            <field name="scheduled_date" readonly="state == 'done'" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Products">
                            <field
                                name="line_ids"
                                readonly="state in ('done', 'cancelled')"
                            >
                                <tree editable="bottom">
                                    <field name="state" column_invisible="1" />
                                    <field name="product_id" />
                                    <field name="product_uom_qty" />
                                    <field name="product_uom" />
                                    <field
                                        name="qty_shipped"
                                        readonly="1"
                                        column_invisible="parent.state == 'draft'"
                                    />
                                    <field
                                        name="qty_received"
                                        readonly="1"
                                        column_invisible="parent.state == 'draft'"
                                    />
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter />
            </form>
        </field>
    </record>

    <record id="view_stock_interwarehouse_transfer_list" model="ir.ui.view">
        <field name="name">stock.interwarehouse.transfer.list</field>
        <field name="model">stock.interwarehouse.transfer</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name" />
                <field name="warehouse_from_id" />
                <field name="warehouse_to_id" />
                <field name="scheduled_date" />
                <field
                    name="state"
                    widget="badge"
                    decoration-success="state == 'done'"
                    decoration-info="state == 'in_transit'"
                    decoration-warning="state == 'confirmed'"
                    decoration-danger="state == 'cancelled'"
                />
            </tree>
        </field>
    </record>

    <record id="action_stock_interwarehouse_transfer" model="ir.actions.act_window">
        <field name="name">Inter-Warehouse Transfers</field>
        <field name="res_model">stock.interwarehouse.transfer</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem
        id="menu_stock_interwarehouse_transfer"
        name="Inter-Warehouse Transfers"
        parent="stock.menu_stock_transfers"
        action="action_stock_interwarehouse_transfer"
        sequence="30"
    />
</odoo>
