This module only sets the field `packaging_id` on `stock.quant.package` from
`delivery` module as invisible, because the field is already added by
`stock_quant_package_product_packaging` module with the correct domain.