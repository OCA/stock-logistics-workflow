This module adds the possibility to apply a general route on a
quotation.

Routes selected on the quotation are propagated to its order lines. When
creating order lines from the backend, existing line-specific routes are
preserved.
