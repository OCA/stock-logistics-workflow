How It Works:

- New pickings created with this operation type will automatically use the configured shipping policy
- Users can still override the shipping policy manually on individual pickings
- Existing pickings are not affected by operation type changes

Example

Set *Receipts* operation type to "As soon as possible" for faster processing, and *Customer Deliveries* to "When all products are ready" for complete shipments.
