This module lets you define rules that automatically process pending stock
pickings (confirm, assign, validate) based on configurable conditions.

It is useful when certain pickings are routinely ready to ship as soon as
they are reserved, and manually clicking through them adds no value.
