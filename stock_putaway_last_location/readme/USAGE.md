Enable `Storage Locations` in `Settings > Inventory > Warehouse` and then activate the
new `Putaway Default To Last Location` option for the location where you want to use the
default putaway location set at the last location used for the product.
