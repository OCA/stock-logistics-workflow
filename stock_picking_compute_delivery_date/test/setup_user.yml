-
  I create a user to run the tests in this module
-
  !record {model: res.users, id: res_users_emma}:
    name: Emma
    login: emma
    email: emma@example.com
-
  Write groups on a write, because create ignores them
-
  !record {model: res.users, id: res_users_emma}:
    groups_id:
      - stock.group_stock_user
