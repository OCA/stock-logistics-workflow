Adds a **Supplier** to lot/serial numbers.

When a receipt (incoming transfer) is validated, the supplier from the
picking is automatically written to all tracked lots/serial numbers
received in that transfer (only if no supplier is already set).

The supplier field can be used to filter and group lots in the Lots/Serial
Numbers list view.
