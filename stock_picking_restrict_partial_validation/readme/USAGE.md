1. Go to *Inventory > Configuration > Operation Types* and open the operation
   type to restrict.
2. Enable *Restrict Partial Validation*.

From then on, transfers of that operation type cannot be validated until they
are fully reserved, and cannot be validated for less than the demanded
quantities. Attempting either shows a blocking message explaining the reason.