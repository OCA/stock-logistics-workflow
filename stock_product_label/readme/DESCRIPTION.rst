Original wizard ProductLabelLayout is using native Odoo field Selection so it
does not allow removing format. Besides that, adding format requires modifying
template. This module refactors the selection and adds the flexibity to update
label layout.
