<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Group for users who can create products - appears in Inventory section -->
        <record model="res.groups" id="group_product_create">
            <field name="name">Criar produtos</field>
            <field name="category_id" ref="base.module_category_inventory_inventory"/>
            <field name="comment">Users in this group can create and modify products</field>
            <field name="implied_ids" eval="[(4, ref('stock.group_stock_user'))]"/>
        </record>
    </data>
</odoo>
